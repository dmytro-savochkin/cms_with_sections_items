- unless @bread_crumbs.nil?
  - @bread_crumbs.each_with_index do |element, index|
    - if index == @bread_crumbs.length - 1
      = element.titleize
    - else
      = link_to element.titleize, link_to_nth_element_of(index, request.fullpath)
      &gt;