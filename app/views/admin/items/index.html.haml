%br

= link_to 'Create new item', new_admin_item_path

%h2 All Items

- place_list_tags(@menu_list).each do |element|
  - if element.kind_of? Section
    = element[:name]

  - elsif element.kind_of? Item
    = link_to element[:name], edit_admin_item_path(element[:id]), :class => link_color?(element[:hidden])
    &nbsp;&nbsp;
    = link_to "&uarr;".html_safe, admin_item_up_path(element) if element[:shifts][:up] if element[:shifts][:up]
    = link_to "&darr;".html_safe, admin_item_down_path(element) if element[:shifts][:down] if element[:shifts][:down]

  - else
    = element

