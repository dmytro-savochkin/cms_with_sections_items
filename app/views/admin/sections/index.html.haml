%br

= link_to 'Create new section', new_admin_section_path

%h2 All Sections

- place_list_tags(@menu_list).each do |element|
  = element unless element.kind_of? Section
  - if element.kind_of? Section
    = link_to element[:name], edit_admin_section_path(element[:id]), :class => link_color?(element[:hidden])
    &nbsp;&nbsp;
    = link_to "&uarr;".html_safe, admin_section_up_path(element) if element.can_be_shifted[:up]
    = link_to "&darr;".html_safe, admin_section_down_path(element) if element.can_be_shifted[:down]

