= link_to 'back', admin_sections_path

%h1 Create Section

= form_for @section, :url => admin_sections_path, :method => :post do |f|

  = render "layouts/admin/section_form", :f => f

  %br
  = submit_tag 'Create Section'
